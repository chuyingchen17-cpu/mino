# 构建产物与缓存，不入库。
dist/
.astro/
node_modules/

# 同步脚本的产物：源在 apps/macos 的 PetFrames，这里只是副本。
# 但落地页要能独立构建部署，所以帧本身入库，只忽略临时文件。
.DS_Store

# 安装脚本的副本，源是仓库根的 Scripts/install.sh，由 sync:install 在构建时生成。
# 这一份不入库：两份 zsh 脚本并存迟早分叉，而分叉的安装脚本会静默装错版本。
public/install.sh
