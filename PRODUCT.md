# Product

## 定位

Mino 是原生 macOS 个人桌宠社交产品。宠物首先属于个人账号，而不是固定关系空间中的装饰；它由账号主设备上的本地 Agent 驱动，在明确的好友授权内自主维系关系。

## 产品原则

1. 个人先于关系：身份、宠物、记忆和个人事件线属于账号。
2. 授权先于互动：只有 `accepted` 的好友才能对话、串门、互动和托信。
3. 决策留在客户端：Worker 协调和代理模型，但不会在客户端离线时伪造宠物回应。
4. 本地永远有反应：动画和明确状态先本地反馈；模型只用于高价值语义决定。
5. 持久事实可恢复：D1 是业务事实来源，WebSocket 只是提示，所有状态都能由 bootstrap + Account Event 重建。
6. 私密内容最小传播：文字信正文不进入事件、日志、幂等回执、Agent observation 或模型上下文。

## 核心体验

- 每个账号一只宠物，可在多个 macOS 设备登录；同一时刻一个设备为 Primary Agent Device。
- 账号可拥有多个好友，申请、接受、拒绝和关闭都是持久化状态。
- 串门是 `Visit` 聚合：访客主人可请求登门，Host 也可邀请好友宠物；系统从方向推导唯一 responder。
- Visit 接受后，访客在原端隐藏、在 Host 端出现；结束后双方恢复。离线 Agent 的待处理互动通过事件和 bootstrap 恢复。
- Feed、play、pet、hug、kiss、flower、walk、message 等真人动作需要访客原端 Agent 回复；服务端不代答。
- 个人事件线聚合所有好友的重要事件，不显示虚构的串门持续时长。
- 文字信在 active Visit 中附着，Visit 结束时只交付一次；只有授权真人界面可以读取正文。

## 技术边界

- 后端为 Cloudflare Worker + Hono + D1 + account-scoped Durable Object。
- Durable Object 每个 Account 一个，只负责同账号多设备的低延迟提示；不保存业务事实。
- D1 保存账号、设备、会话哈希、宠物、好友、Visit、动作、对话、密文信件、账号事件、幂等回执和模型推理结果。
- 屏幕坐标、动画状态和本地 Agent 长期记忆不会上传为服务端事实。
- 宠物形象只接受版本化目录中的白名单字段，不允许任意文件路径或 URL。

## 当前不包含

账号搜索与推荐、好友数量策略、拉黑举报、系统推送、图片明信片、物品经济、完整 E2EE、生产监控告警、发行签名和自动更新。开发环境提供 Alice / Bob / Charlie 固定身份；生产环境必须关闭开发 bootstrap，并使用 GitHub Device Flow。QQ 登录待 QQ 互联开发者资质、应用和回调域名审核完成后接入。

## 无障碍

好友、Visit、离线和同步状态不能只依赖颜色表达。主要操作需要明确文字、键盘焦点和 VoiceOver 标签。
